// src/category/repositories/project-category.repository.ts

import { Repository, DataSource } from 'typeorm';
import { ProjectCategory } from '@category/entities/project-category.entity';

class ProjectCategoryRepository extends Repository<ProjectCategory> {
    constructor(private dataSource: DataSource) {
        super(ProjectCategory, dataSource.createEntityManager());
    }
}

export default ProjectCategoryRepository;